# 系统BUG体检报告（2026-07-02 全局快扫）

检查手段：`tsc --noEmit` 全量编译、密钥/危险SQL/分层违规 grep 扫描、核心脚本人工通读、git 状态核查。每条均已核实到具体文件行号。

---

## P0 — 立即处理

### 1. aiDailyDiagnosis.ts 中文弯引号 → 编译失败，AI每日诊断+每日自动化链条全断
- 位置：`src/aiDailyDiagnosis.ts:1287-1288`
- 现状：`{ role: “system”, ... }` / `{ role: “user”, ... }` 用了中文弯引号，非法字符，整个文件无法编译（tsc 报 20 个错误，全部源于此）。
- 影响：`npm run ai:daily` 启动即崩；`runDailyAutomation.ts` 中 `assertStepSuccess(ai)` 抛错 → **每日自动化在 AI 步骤后整体标记失败**（aiEnabled 且非 core-only 时）。
- 来源：未提交的本地改动（git status 显示 M）。
- 修复：两处弯引号改直引号 `"system"` / `"user"`。
- 验收：`npx tsc --noEmit` 0 错误；`npm run ai:daily -- --help` 可启动。
- 风险：无，纯语法修复。

### 2. 真实API密钥已提交进Git仓库
- 位置：`.envvvvvvfdfd`（被 git 跟踪，已提交）
- 内容：真实 `LINGXING_APP_ID=ak_zadV...` 和 `LINGXING_APP_SECRET`，**与当前 .env 中生产密钥完全相同**，仍然有效。
- 影响：仓库若推送到任何远端（GitHub/Gitee/公司Git），领星ERP密钥即泄漏，可被拉取全部经营数据。
- 修复：① `git rm --cached .envvvvvvfdfd` 并删除文件；② 历史已含密钥，需用 `git filter-repo` 清历史或直接**在领星后台轮换密钥**（推荐，最简单可靠）；③ 轮换后更新 .env。
- 验收：`git ls-files | grep -i env` 只剩 `.env.example`；旧 secret 调用领星API返回鉴权失败。

---

## P1 — 尽快处理

### 3. RAW层 DELETE-then-INSERT 无事务，中途失败=原始数据丢失
- 位置：
  - `src/syncLingxingToRawFeishu.ts:298`（clearAll=true 时**整表范围删除**）、303、368
  - `src/syncOrderProfitDaily.ts:323`
- 现状：先 `DELETE FROM raw_feishu_table ...` 再分批 INSERT，全程无 `beginTransaction/commit/rollback`。插入中途报错（网络/数据格式），已删数据无法恢复。
- 影响：违反中台规范"RAW留存原始数据、不可清空数据表"；<REDACTED_FEISHU_SHEET_ID> 全量覆盖模式下风险最大。
- 修复建议（二选一）：① 包事务，失败 rollback；② 改为写临时批次+校验成功后按 batch_id 切换/删旧。
- 验收：模拟插入中途抛错，RAW 表数据不丢。

### 4. 前端/通知直查RAW层，违反分层规范
- 位置：`src/feishuRawSalesRoutes.ts`（10+ 处 `FROM raw_feishu_table`，直接服务前端接口）；`src/noOrderNotify.ts`、`src/lowProfitNotify.ts` 亦读 `raw_feishu_table`。
- 影响：违反"前端仅读取DIM/FACT/EVENT/AI层"。RAW 结构一变（row_json 字段名改动），前端和通知直接挂，且无缓冲层。
- 修复建议：属历史遗留，不建议一次性重写；新功能一律走 DIM/FACT，旧接口排期迁移。
- 风险标注：迁移涉及前端展示口径，需逐接口对数验收。

### 5. adminServer 密码保护 fail-open
- 位置：`src/adminServer.ts:33、108`
- 现状：`ADMIN_PASSWORD` 环境变量未配置时中间件直接 `next()` 放行，**整个管理后台无密码裸奔**。
- 修复：未配置密码时启动即报错退出（fail-closed），或至少打印醒目警告。
- 验收：不设 `ADMIN_PASSWORD` 启动 `admin:server` 应拒绝启动或所有请求 401。

---

## P2 — 择期清理

### 6. package.json 有4个脚本指向已删除的文件
`test:connection`→src/testConnection.ts、`probe:lingxing`→src/probeLingxingApi.ts、`fix:yuesi-cs-costs`、`init:ai-config` 均指向不存在的文件，运行即报错。删脚本条目或恢复文件。

### 7. 未提交的部署文件删除 + 历史垃圾文件
- 本地已删除但未提交：`ecosystem.config.js`（pm2）、`scripts/install-daily-automation-cron.sh`（cron安装）。**若这是误删，提交后会影响定时任务部署**——请确认是否有意为之（对应铁律"无指令不动定时任务"）。
- 仓库历史曾误提交垃圾文件：`--method=POST`、`lingxing-erp-api-connection-test@0.1.0`、`ts-node`（命令输出当文件提交了），本地已删，提交即可清掉。

### 8. @types/node 12.20.55 过旧
与 ts-node 10 / express 5 / mysql2 3.x 不匹配，可能掩盖类型错误。建议升到 @types/node 18+（配合服务器 Node 版本）。

---

## 已查无恙项
- SQL 注入：抽查的动态 WHERE 均为 `?` 参数化拼接，未见字符串直拼用户输入。
- 时区：各同步脚本取"今天"均已做 UTC+8 或 Intl Asia/Shanghai 处理，未见 UTC 裸用陷阱。
- ON DUPLICATE KEY UPDATE：未见覆盖运营人工备注字段的写法。
- `.env` 本身未被 git 跟踪，.gitignore 已含 `.env`。
