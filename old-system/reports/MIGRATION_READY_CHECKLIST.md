# 迁移就绪检查

| 检查项 | 结果 |
|---|---|
| 是否仍有 .DS_Store | 否（通过） |
| 是否仍有 __MACOSX | 否（通过） |
| 是否仍有 .env / .env.* | 否（通过） |
| 是否仍有 SQL 文件 | 否（通过） |
| 是否仍有 数据库文件 | 否（通过） |
| 是否仍有 密钥文件 | 否（通过） |
| 是否仍有 node_modules/dist/build | 否（通过） |
| 是否保留 `.agents/` | 否（按建议排除） |
| 是否建议迁入 `SZZSBJ/old-system/source/` | 条件性建议：完成资源 ID、基础设施端点和历史脚本人工复查后再迁入 |

结论：clean source 已通过机械禁用项检查，但仍应以报告为清单进行人工复查。
