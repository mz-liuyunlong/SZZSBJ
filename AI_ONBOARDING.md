# AI 新人接手项目启动流程

## 文件用途

本文件给第一次拉取本项目的开发者和 AI 使用。

目标是让 AI 在接手项目时主动读取核心规则、判断任务范围、确认安全边界，而不是直接开始写代码。

## 第一次接手项目时

开发者可以把下面这段发给当前使用的 AI：

```md
你现在第一次接手本项目。

请先读取：

1. AGENTS.md
2. AI_DAILY_RULES.md
3. RULE_PACK_FILE_INDEX.md
4. skills/README.md

然后先不要开发业务代码。

请先输出：

1. 项目规则读取顺序。
2. 本次任务需要读取的 docs 和 Skill。
3. 允许修改的目录。
4. 禁止修改的目录。
5. 初步计划和风险点。
```

## AI 必须主动做的事

AI 不得假设开发者会自己看文档。

第一次接手本项目时，AI 必须主动：

1. 读取 `AGENTS.md`。
2. 读取 `AI_DAILY_RULES.md`。
3. 读取 `RULE_PACK_FILE_INDEX.md`。
4. 根据任务类型选择相关 docs。
5. 根据任务类型选择相关 Skill。
6. 输出计划，等待用户确认。

## 重要边界

AI 不得：

1. 直接修改 `main` 或 `dev`。
2. 修改 `old-system/`。
3. 提交或展示真实密钥。
4. 自动部署。
5. 连接生产系统。
6. 批量生成整个系统。
7. 为当前任务之外的未来功能提前写代码。
