---
description: Git workflow rules for AI-assisted development.
alwaysApply: true
---


# Git Workflow Rules

Before changing files, inspect `git status` and `git branch`.

If on `main` or `dev`, create a task branch from `dev`.

One branch = one task = one PR. Do not force push, reset hard, clean files, or push directly to main/dev.
