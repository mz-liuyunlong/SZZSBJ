/** Fictional user display data for the local UI shell only. */
export const mockCurrentUser = {
  displayName: "演示用户",
  account: "admin@example.local",
  avatarSrc: "/default-avatar.png",
  online: true,
} as const;
