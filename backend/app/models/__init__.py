"""Persistence models shared by integration foundations.

Feature models remain in their bounded modules and are imported by Alembic's
environment so every table still belongs to the project's single Base metadata.
"""
