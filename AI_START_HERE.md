# AI 从这里开始

AI 接手本项目时，先不要直接改代码。

## 必读入口

按顺序读取：

```text
AGENTS.md
AI_DAILY_RULES.md
RULE_PACK_FILE_INDEX.md
skills/README.md
```

然后根据任务类型读取相关 docs 和 Skill 文件。

## 修改代码前必须输出

1. 本次任务类型。
2. 允许修改的路径。
3. 禁止触碰的路径。
4. 准备读取的 docs。
5. 准备读取的 Skill。
6. 初步实施计划。
7. 风险点和需要用户确认的事项。

用户确认前，不得修改代码。

## 长任务规则

复杂功能、跨模块任务、跨前后端任务，必须先走 PRP 或任务计划流程。

PRP 是计划，不是批准开发。
