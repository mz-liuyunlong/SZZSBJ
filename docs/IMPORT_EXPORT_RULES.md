# 导入导出规则

## 文件用途

本文件用于约束 Excel / CSV 等导入导出功能。

## 导入规则

导入必须有模板版本、字段校验、错误行返回、任务记录；不能直接覆盖重要数据；大文件导入必须走 Celery；导入结果必须可下载错误报告。

## 导出规则

财务数据导出必须有权限；大文件导出必须走 Celery；导出字段必须明确；导出结果不得包含密钥或敏感配置；导出任务必须记录操作者。
