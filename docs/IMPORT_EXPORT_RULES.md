# 导入导出规则

## 文件用途

本文件用于约束 Excel / CSV 等导入导出功能。

## 导入规则

1. 导入必须有模板版本。
2. 导入必须校验字段。
3. 导入必须返回错误行。
4. 导入必须记录任务。
5. 导入不能直接覆盖重要数据。
6. 大文件导入必须走 Celery。
7. 导入结果必须可下载错误报告。

## 导出规则

1. 财务数据导出必须有权限。
2. 大文件导出必须走 Celery。
3. 导出字段必须明确。
4. 导出结果不得包含密钥或敏感配置。
5. 导出任务必须记录操作者。
