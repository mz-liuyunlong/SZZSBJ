# 表单校验规则

## 文件用途

本文件用于约束表单字段校验和错误提示。

## 基础规则

1. 必填字段必须有明确提示。
2. 金额字段必须校验数字、范围和币种。
3. 日期区间必须校验开始时间不晚于结束时间。
4. SKU / MSKU 搜索必须说明支持的匹配方式。
5. 批量输入必须说明分隔符和最大数量。
6. 错误提示必须可理解，不显示技术堆栈。
7. 提交中必须防重复提交。

## 高风险表单

涉及广告预算、出价、价格、库存、财务、权限的表单必须有二次确认和操作日志。
