# AI 技术开发规则

## 文件用途

本文件用于约束 AI 在本项目中的技术实现方式。

## 总原则

AI 必须遵守固定技术栈，先复用已有模式，再新增方案；先做小范围修改，再考虑抽象；不自行更换技术栈；不新增第二套架构；不随意新增依赖；不把业务逻辑堆在页面或 API Route 中；不复制 `old-system/` 代码；新增代码必须有维护型注释；完成后必须说明风险和测试结果。

## 新依赖规则

新增依赖前必须说明：为什么现有依赖不能解决、新依赖解决什么问题、是否长期维护、是否影响构建体积、是否有安全风险、是否可以不用新增依赖实现。

未获确认前，不得新增依赖。
