# 文档更新规则

## 文件用途

本文件用于约束 AI 在新增功能后同步更新项目文档。

## 规则

新增页面时，AI 应更新路由/菜单说明、API 契约、页面需求文档、组件清单。

新增后端模块时，AI 应更新后端模块清单、API 契约、数据库说明。

新增 Celery 任务时，AI 应更新后台任务规则或任务清单。

新增重大架构决策时，AI 应新增 ADR。

AI 不允许只改代码不更新相关文档，除非用户明确说明本次只做临时实验。
